#!/bin/sh
# TC commands need the 'tc-' prefix in bash.
# (tc-alias.sh is expected to be in the PATH)
. tc-alias.sh

param select parameters.bson

dataman start

simulator_mavlink start
tone_alarm start
pwm_out_sim start

tests @test_name@

shutdown
